-- ================================================================
-- SQL Script: إلغاء / حذف أحدث المدفوعات المسجلة
-- تشغيل هذا الملف في Supabase SQL Editor عند الحاجة لحذف أحدث مدفوعات
-- ================================================================

-- 1. حذف المدفوعات التي تم تسجيلها اليوم (تاريخ اليوم)
DELETE FROM payments WHERE payment_date = CURRENT_DATE;

-- 2. أو حذف أحدث 10 مدفوعات مسجلة على النظام (يمكنك إلغاء التعليق عن السطر التالي لترفيعه)
-- DELETE FROM payments WHERE id IN (SELECT id FROM payments ORDER BY created_at DESC LIMIT 10);
